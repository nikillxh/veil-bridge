[package]
name = "bridge-program"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "bridge-program"
path = "src/main.rs"

[dependencies]
sp1-zkvm = "4.1.0"
bridge-core = { path = "../bridge-core", default-features = false }
